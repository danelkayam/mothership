name: donnager

include:
  - ./common.yaml

services:
  n8n:
    image: docker.n8n.io/n8nio/n8n:latest
    env_file:
      - ./envs/common.env
      - ./envs/n8n.env
    volumes:
      - n8n_data:/home/node/.n8n
    restart: unless-stopped

volumes:
  n8n_data:
