watch:
  workers: 10
  schedule: "0 */6 * * *"
  firstCheckNotif: true
  compareDigest: true

notif:
  script:
    cmd: '/notify.sh'

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    watchByDefault: true